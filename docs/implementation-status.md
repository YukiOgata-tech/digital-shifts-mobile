# Dミセ モバイル実装状況

最終確認日: 2026-07-24

## 判定

- **接続済み**: Supabaseまたは端末APIまで接続済み
- **一部接続**: 初回リリースに必要な主要経路は接続済みだが、補助機能が残る
- **未実装**: 画面または処理が未着手

## 共通基盤

| 項目 | 状況 | 内容 |
|---|---|---|
| Expo / ナビゲーション | 接続済み | Expo SDK 57、Expo Router、Native Tabs / Stack / Modal |
| ネイティブUI | 接続済み | `@expo/ui`、Haptics、OS標準権限ダイアログ |
| ブランドアセット | 接続済み | Web版Dミセロゴを`assets/images/brand`へ取り込み、Splash/Icon/Faviconへ設定 |
| Supabase Auth | 接続済み | メール・パスワード、認証ガード、復元、自動更新、ログアウト |
| セッション保存 | 接続済み | ネイティブはSecureStore分割保存、旧AsyncStorageから自動移行。WebのみAsyncStorage |
| データ取得 | 接続済み | TanStack Query、NetInfo連携、再接続時再取得、Pull to Refresh |
| 所属コンテキスト | 接続済み | プロフィール、テナント、店舗、複数所属切替、端末内の選択保存 |
| 環境変数 | 接続済み | モバイル公開値とサーバー秘密値のexampleを分離 |

## スタッフ機能

| 機能 | 状況 | 接続済みの範囲 | 残作業 |
|---|---|---|---|
| ログイン / ログアウト | 接続済み | Supabase Auth、未ログインルート保護 | パスワード再設定導線 |
| スタッフホーム | 接続済み | 本人・店舗、今日/直近シフト、提出期限、最新通知 | ホーム用集約RPCによる通信回数削減 |
| 希望シフト | 接続済み | 受付期間、休業日、終日/時間指定、希望種別、メモ、原子的保存、提出、下書き戻し | 店舗定義済み時間帯の選択UI |
| 公開シフト | 接続済み | 公開済み本人シフト、店舗・役割・休憩 | 月表示、過去月移動 |
| 打刻 | 接続済み | 前景GPS、範囲判定、例外理由、出勤/退勤/休憩、未終了休憩補完、要確認化 | 実機で権限拒否/低精度/日跨ぎ検証 |
| 勤怠の後から入力 | 接続済み | 日付・時刻・休憩・理由、重複防止、要確認、監査ログ | 既存レコードの編集・履歴一覧 |
| 通知 | 接続済み | 一覧、未読件数、個別/一括既読、Realtime反映 | ページング、通知リンクの個別詳細遷移 |
| Push通知 | 一部接続 | Androidチャンネル、権限要求、Expo Push Token取得、端末別RLS保存、通知タップ遷移 | EAS Project ID、APNs/FCM credentials、送信ワーカー接続 |
| ヘルプ募集 | 接続済み | 所属店舗の募集中一覧、本人応募・状態表示 | モバイル応募時のメール送信をWeb/Edge共通サービスへ統合 |
| プロフィール | 接続済み | 表示名・電話番号更新、所属切替、通知設定、外部規約リンク | アバター更新 |
| メール通知設定 | 一部接続 | Web版のDB設定を表示、他通知設定保存時も値を保持 | 契約entitlement判定が必要なため、変更はサーバーAPI接続後 |
| 新規登録・招待参加 | 未実装 | 既存アカウントのログインを優先 | 初回リリース対象か決定が必要 |

## Supabaseへ追加済み

`D-mise`プロジェクトへ次のマイグレーションを適用済みです。

- `mobile_push_tokens`: 本人のみが自分の端末トークンを管理できるRLS付きテーブル
- `record_mobile_attendance_event`: 打刻レコードとイベントを1トランザクションで更新する認証必須RPC
- `submit_mobile_manual_attendance`: 重複検証・イベント・監査ログをまとめる認証必須RPC
- anonロールから上記RPCの実行権限を明示的に剥奪

ローカルの正本は`supabase/migrations/`にあります。共有DBを利用するWeb版リポジトリにも同じマイグレーション履歴と生成型を同期する必要があります。

## 既知の制約

- Push送信処理自体は、Web版の通知作成処理またはSupabase Edge FunctionからExpo Push Serviceを呼ぶ統合が必要です。秘密値はモバイルへ入れません。
- メール送信は既存Web版のサーバー責務です。Resend/SMTPの秘密値は`supabase/.env.example`に例示し、Expo公開envから分離しています。
- 管理者向けシフト作成、給与、請求、developerコンソール、PWA導線はスタッフ向けネイティブ初回リリースの対象外です。
- シミュレータは今回起動していません。実データでの端末操作確認は、テストアカウントと実機/EAS設定後に行います。

## 次の優先作業

1. 正式なBundle Identifier / Android Application IDとEAS Project IDを確定
2. APNs / FCM credentialsをEASへ設定し、Push送信ワーカーをWeb版通知サービスへ接続
3. 実機でログイン、GPS打刻、権限拒否、Push通知タップを検証
4. 勤怠履歴・既存レコード編集、月間シフト表示を追加
5. Web版リポジトリへSupabase migrationと生成型を同期
